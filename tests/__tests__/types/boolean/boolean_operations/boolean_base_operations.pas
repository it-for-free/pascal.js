
var a, b, e, f, g, h, m, n, k, y: boolean;
begin  
   a := true;
   b := false;

   e := true and true;
   f := true and false;
   g := true or b;
   h := a or b;
   m := a and b;
   n := h or a and b;
   k := not b;
   y := not a;
end.